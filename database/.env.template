# =============================================================================
# DATABASE CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env and customize with your actual values
# Command: cp .env.template .env

# =============================================================================
# MONGODB ROOT CONFIGURATION
# =============================================================================
# MongoDB root admin credentials for database administration
MONGO_INITDB_ROOT_USERNAME=your_mongo_username
MONGO_INITDB_ROOT_PASSWORD=your_secure_mongo_password

# Application database name
MONGO_INITDB_DATABASE=your_database_name

# =============================================================================
# APPLICATION ADMIN USER CONFIGURATION
# =============================================================================
# Admin user email for application login
ADMIN_EMAIL=admin@yourdomain.com

# Admin password hash generated with bcrypt
# Generate with: cd ../backend && node -pe "require('bcryptjs').hashSync('YOUR_PASSWORD', 12)"
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy: cp .env.template .env
# 2. Replace ALL placeholder values with your actual values
# 3. For MongoDB: username is usually 'root', change password
# 4. Generate admin hash with provided command
# 5. Never commit .env file
# =============================================================================