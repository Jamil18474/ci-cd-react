# ===================================================================
# TEMPLATE DE CONFIGURATION ENVIRONNEMENT
#
# INSTRUCTIONS:
# 1. Copier ce fichier vers .env
# 2. Choisir la configuration MongoDB selon votre environnement
# 3. Remplir toutes les autres valeurs
# 4. Générer les secrets avec les commandes fournies
# ===================================================================

# ============================================================================
# CONFIGURATION MONGODB - CHOISIR SELON ENVIRONNEMENT
# ============================================================================

# 🏠 DÉVELOPPEMENT LOCAL (Docker/MongoDB local)
# MONGODB_URI=mongodb://ROOT_USERNAME:ROOT_PASSWORD@localhost:27017/YOUR_DATABASE?authSource=admin

# 🌐 PRODUCTION (MongoDB Atlas - Recommandé)
# MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# ⚠️  DÉCOMMENTEZ ET ADAPTEZ LA LIGNE CORRESPONDANT À VOTRE ENVIRONNEMENT
MONGODB_URI=CHOISIR_ET_ADAPTER_UNE_CONFIG_CI_DESSUS

# ============================================================================
# CONFIGURATION JWT - CRITIQUE
# ============================================================================

# JWT Secret - OBLIGATOIRE À GÉNÉRER
# 🔐 Générer avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 🔐 Ou avec: openssl rand -hex 64
# ⚠️  Minimum 32 caractères, recommandé 128+ caractères
# ⚠️  DIFFÉRENT entre développement et production !
JWT_SECRET=GENERER_AVEC_COMMANDE_CRYPTO_RANDOMYTES

# Durée de validité des tokens JWT (en secondes)
# 86400 = 24h (développement), 3600 = 1h (production recommandée)
JWT_EXPIRES_IN=86400

# ============================================================================
# CONFIGURATION SÉCURITÉ
# ============================================================================

# Configuration bcrypt (rounds de hashage)
# 12 = recommandé 2025 (compromis sécurité/performance)
BCRYPT_ROUNDS=12

# ============================================================================
# CONFIGURATION SERVEUR
# ============================================================================

PORT=5000

# Environnement d'exécution
# development | production | staging
NODE_ENV=development

# ============================================================================
# CONFIGURATION CORS
# ============================================================================

# URL du frontend autorisé
FRONTEND_URL=http://localhost:3000

